# === Rust ===
# Cargo build output
target

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# Coverage report written by `mise run coverage-lcov` (uploaded as a CI artifact)
lcov.info

# === FFI ===
# UniFFI-generated language bindings (reproducible from the cdylib via
# `mise run bindings`; distributed via PyPI/Maven/SwiftPM, not committed here).
/bindings/

# Python wheels written by maturin (`--out dist` in release-python.yml).
/crates/geocoordinates-ffi/dist/

# Swift XCFramework build outputs (reproducible; the zip ships on the GitHub
# Release, the generated Swift sources under swift/Sources/ are committed).
/swift/*.xcframework/
/swift/*.xcframework.zip
